# Исправления безопасности

## ✅ Исправленные проблемы

### 1. JWT Secret - КРИТИЧНО ИСПРАВЛЕНО
- **Было:** Дефолтный ключ `'your-secret-key'` если переменная окружения не установлена
- **Стало:** Обязательная проверка наличия `JWT_SECRET` в переменных окружения
- **Файлы:** `server/middleware/auth.js`, `server/controllers/authController.js`
- **Действие:** Убедитесь, что в `.env` файле установлен `JWT_SECRET` с криптографически стойким ключом (минимум 32 символа)

### 2. httpOnly Cookies - КРИТИЧНО ИСПРАВЛЕНО
- **Было:** `httpOnly: false` - токены доступны через JavaScript
- **Стало:** `httpOnly: true` - защита от XSS атак
- **Дополнительно:** `sameSite: 'strict'` - защита от CSRF
- **Файлы:** `server/controllers/authController.js`, `client/src/context/AuthContext.tsx`
- **Примечание:** Клиент обновлен для работы с httpOnly cookies

### 3. Срок жизни токена - ИСПРАВЛЕНО
- **Было:** 365 дней
- **Стало:** 7 дней (настраивается через `JWT_EXPIRES_IN`)
- **Файл:** `server/controllers/authController.js`

### 4. Rate Limiting - ДОБАВЛЕНО
- **Добавлено:** Защита от брутфорса для эндпоинтов аутентификации (5 запросов за 15 минут)
- **Добавлено:** Общий rate limiting (100 запросов за 15 минут)
- **Файлы:** `server/index.js`, `server/routes/auth.js`
- **Зависимость:** `express-rate-limit`

### 5. Валидация email - УЛУЧШЕНО
- **Было:** Только проверка `endsWith('@rwb.ru')`
- **Стало:** Строгая регулярная проверка `/^[a-zA-Z0-9._-]+@rwb\.ru$/`
- **Файл:** `server/controllers/authController.js`

### 6. Валидация кода подтверждения - ДОБАВЛЕНО
- **Добавлено:** Проверка формата кода (6 цифр)
- **Файл:** `server/controllers/authController.js`

### 7. Валидация типов данных - ДОБАВЛЕНО
- **Добавлено:** Проверка типов для `deskId` и других параметров
- **Файл:** `server/controllers/bookingsController.js`

### 8. Security Headers (Helmet) - ДОБАВЛЕНО
- **Добавлено:** Защита заголовков безопасности через helmet.js
- **Файл:** `server/index.js`
- **Зависимость:** `helmet`

### 9. Ограничение размера запроса - ДОБАВЛЕНО
- **Добавлено:** Лимит 10MB для тела запроса
- **Файл:** `server/index.js`

## 📦 Новые зависимости

Установите новые зависимости:

```bash
cd server
npm install express-rate-limit helmet
```

## 🔧 Настройка переменных окружения

Убедитесь, что в `.env` файле установлены следующие переменные:

```env
# ОБЯЗАТЕЛЬНО: Криптографически стойкий ключ (минимум 32 символа)
JWT_SECRET=your-very-long-and-secure-secret-key-here-minimum-32-characters

# Опционально: Срок жизни токена (по умолчанию 7d)
JWT_EXPIRES_IN=7d

# Опционально: Срок жизни токена в миллисекундах для cookie (по умолчанию 604800000 = 7 дней)
JWT_EXPIRES_IN_MS=604800000

# URL клиентского приложения
CLIENT_URL=http://localhost:3000

# Остальные переменные...
```

## ⚠️ Важные замечания

1. **JWT_SECRET:** НЕ используйте дефолтное значение в production! Сгенерируйте криптографически стойкий ключ:
   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```

2. **httpOnly cookies:** Токены теперь недоступны через JavaScript на клиенте. Это повышает безопасность, но требует обновления клиентского кода (уже сделано).

3. **Rate limiting:** В production может потребоваться настройка через Redis для распределенных систем.

4. **CORS:** Убедитесь, что `CLIENT_URL` правильно настроен для production окружения.

## 🧪 Тестирование

После установки зависимостей и настройки переменных окружения:

1. Перезапустите сервер
2. Проверьте, что аутентификация работает
3. Убедитесь, что rate limiting работает (попробуйте сделать более 5 запросов к `/api/auth/request-code`)

## 📝 Оставшиеся рекомендации (не критично)

Следующие улучшения можно добавить в будущем:
- CSRF токены (частично решено через sameSite=strict)
- Более детальное логирование безопасности
- Мониторинг подозрительной активности
- Двухфакторная аутентификация
- Refresh tokens для более безопасного обновления токенов

