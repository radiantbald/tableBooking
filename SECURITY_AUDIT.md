# Отчет по безопасности кода

## 🔴 Критические уязвимости

### 1. Слабый JWT Secret с дефолтным значением
**Файлы:** `server/middleware/auth.js:17`, `server/controllers/authController.js:172,248`
- **Проблема:** Используется дефолтный секретный ключ `'your-secret-key'` если переменная окружения не установлена
- **Риск:** Злоумышленник может подделать JWT токены
- **Решение:** Обязать установку `JWT_SECRET` в production, использовать криптографически стойкий ключ

### 2. Cookies с httpOnly: false
**Файлы:** `server/controllers/authController.js:188,256`
- **Проблема:** Токены доступны через JavaScript (`httpOnly: false`)
- **Риск:** Уязвимость к XSS атакам - злоумышленник может украсть токены через JavaScript
- **Решение:** Установить `httpOnly: true` и использовать отдельный механизм для чтения токена на клиенте

### 3. Слишком долгий срок жизни токена
**Файл:** `server/controllers/authController.js:249`
- **Проблема:** Токены действительны 365 дней
- **Риск:** При компрометации токена злоумышленник имеет доступ на год
- **Решение:** Уменьшить срок жизни до 7-30 дней, добавить refresh tokens

## 🟠 Высокий приоритет

### 4. Отсутствие Rate Limiting
**Файлы:** `server/controllers/authController.js:127,196`
- **Проблема:** Нет ограничений на частоту запросов кода подтверждения
- **Риск:** Брутфорс атаки, спам email, DoS
- **Решение:** Добавить rate limiting (express-rate-limit)

### 5. Слабая валидация email
**Файлы:** `server/controllers/authController.js:131,200`
- **Проблема:** Проверка только через `email.endsWith('@rwb.ru')`
- **Риск:** Можно обойти проверку (например, `test@rwb.ru.evil.com`)
- **Решение:** Использовать регулярное выражение или библиотеку валидации

### 6. Отсутствие валидации типов данных
**Файлы:** `server/controllers/bookingsController.js:18`, `server/controllers/desksController.js:5`
- **Проблема:** Нет проверки типов для `deskId`, `date` и других параметров
- **Риск:** SQL injection через неожиданные типы данных, ошибки приложения
- **Решение:** Добавить строгую валидацию с помощью express-validator или joi

### 7. Отсутствие CSRF защиты
**Файл:** `server/index.js`
- **Проблема:** Нет защиты от CSRF атак
- **Риск:** Злоумышленник может выполнять действия от имени пользователя
- **Решение:** Добавить csrf middleware или использовать SameSite=Strict для cookies

### 8. Отсутствие защиты заголовков безопасности
**Файл:** `server/index.js`
- **Проблема:** Нет helmet.js для установки security headers
- **Риск:** Уязвимости к XSS, clickjacking, MIME sniffing
- **Решение:** Добавить helmet.js

## 🟡 Средний приоритет

### 9. Логирование чувствительной информации
**Файл:** `server/controllers/authController.js:54-95`
- **Проблема:** Коды подтверждения выводятся в консоль
- **Риск:** Утечка данных через логи
- **Решение:** Логировать только в режиме разработки или использовать безопасное логирование

### 10. Отсутствие ограничения размера тела запроса
**Файл:** `server/index.js:18`
- **Проблема:** Нет ограничения на размер JSON тела запроса
- **Риск:** DoS атаки через большие запросы
- **Решение:** Добавить лимит через express.json({ limit: '10mb' })

### 11. CORS настроен слишком широко
**Файл:** `server/index.js:13-16`
- **Проблема:** Разрешены credentials для любого origin из переменной окружения
- **Риск:** Если переменная окружения не установлена, используется localhost, что может быть проблемой
- **Решение:** Убедиться, что в production установлен правильный origin

### 12. Отсутствие валидации формата даты
**Файлы:** `server/controllers/bookingsController.js:30`, `server/controllers/desksController.js:15`
- **Проблема:** Проверка только через `isNaN(dateObj.getTime())`
- **Риск:** Принимаются невалидные даты, которые могут привести к ошибкам
- **Решение:** Использовать более строгую валидацию формата даты

### 13. Отсутствие sanitization входных данных
- **Проблема:** Нет очистки входных данных от потенциально опасных символов
- **Риск:** XSS через сохраненные данные (хотя используется параметризованные запросы для SQL)
- **Решение:** Добавить sanitization для всех пользовательских данных

## ✅ Положительные моменты

1. ✅ Использование параметризованных SQL запросов (защита от SQL injection)
2. ✅ Использование транзакций для критических операций
3. ✅ Проверка прав доступа (пользователь может отменить только свои бронирования)
4. ✅ Проверка существования ресурсов перед операциями
5. ✅ Использование HTTPS в production для cookies (secure flag)

## Рекомендации по исправлению

1. **Немедленно:** Исправить JWT_SECRET и httpOnly cookies
2. **В ближайшее время:** Добавить rate limiting и валидацию
3. **В следующей итерации:** Добавить CSRF защиту, helmet, улучшить валидацию

